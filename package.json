{
  "name": "speak-to-kit-website",
  "version": "0.1.0",
  "private": true,
  "description": "Marketing site for Speak to Kit. Static HTML served from /public.",
  "scripts": {
    "dev": "serve public -l 3000",
    "deploy": "gh-pages -d public -b gh-pages --dotfiles",
    "screenshots:baseline": "node scripts/screenshots.mjs baseline",
    "screenshots:after": "node scripts/screenshots.mjs after"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "playwright": "^1.48.0",
    "serve": "^14.2.3"
  }
}
