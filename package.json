{
  "name": "speak-to-kit-website",
  "version": "0.1.0",
  "private": true,
  "description": "Marketing site for Speak to Kit. Static HTML served from the repo root via GitHub Pages on main.",
  "scripts": {
    "dev": "serve . -l 3000",
    "screenshots:baseline": "node scripts/screenshots.mjs baseline",
    "screenshots:after": "node scripts/screenshots.mjs after"
  },
  "devDependencies": {
    "playwright": "^1.48.0",
    "serve": "^14.2.3"
  }
}
